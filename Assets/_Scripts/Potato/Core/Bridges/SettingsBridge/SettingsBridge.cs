using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Core.Potato
{
    public class SettingsBridge : MonoBehaviour
    {


        // set public data from system values on init
        // if savedata exists, use that instead
        // use PlayerPrefs for settings

        // respond to requests
            // performs requested operation
            // updates public data to reflect state change
    }
}