using UnityEngine;

namespace Potato.FPS.UI
{
    public class PlayerEnergyBar : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}